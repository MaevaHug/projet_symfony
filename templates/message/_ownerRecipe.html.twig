<form method="post" action="{{ path('user_recipe_delete', {'id': recipe.id}) }}">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe.id) }}">
	<button class="btn btn-sm btn-success round">
		<i class="bi bi-check-lg"></i>
	</button>
</form>